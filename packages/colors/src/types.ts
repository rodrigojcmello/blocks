export type RGB = [red: number, green: number, blue: number, alpha: number];

/** HSL color */
export type HSL = [
  hue: number,
  saturation: number,
  lightness: number,
  alpha: number
];

/** Hexadecimal color */
export type HexColor = string;
