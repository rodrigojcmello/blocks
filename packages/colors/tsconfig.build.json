/*
  Jest does not handle well with more than one tsconfig.json, so the tsconfig of this
  module has been renamed to tsconfig.build.json and is referenced directly in the
  build script
*/

{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "lib",
  },
  "include": ["src"],
  "exclude": [
    "src/name/origins"
  ]
}
